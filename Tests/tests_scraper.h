#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <assert.h>
#include <stdbool.h>

#include "../Src/scraper.c"


// Testing that the file is not empty
int getFileTEST();


// Testing that the line is not empty & that the function gets the data right
int getLineTEST();


// Testing that the size is correct
int getSizeTEST();


// Testing that the functions are not empty & that the function gets the data right
int getStructsTEST();


// Testing that the function returns the correct value
int switchStrTEST();


// Testing that the function sets the structure correctly
int setStructTEST();